import aResultModel from "../models/AcumulateResult";
import battleModel from "../models/Battle";
import cardModel from "../models/Cards";
import resultModel from "../models/Result";

export default class BattleController{
    static game = (req, res) => {
        const playerOneCard = req.body;
        const playerTwoCard = req.body;

    }
}